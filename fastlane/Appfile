json_key_file("path/to/play-store-credentials.json") # Path to the json secret file - Follow https://docs.fastlane.tools/actions/supply/#setup to get one
package_name("com.shopeefood") # e.g. com.krausefx.app

# For more information about the Appfile, see:
#     https://docs.fastlane.tools/advanced/#appfile

# Optional: Set the app identifier for iOS if you have a cross-platform project
# app_identifier("com.shopeefood.ios")

# Optional: Set the Apple ID if you have iOS app
# apple_id("[[APPLE_ID]]")

# Optional: Set the team ID if you have iOS app
# team_id("[[DEV_PORTAL_TEAM_ID]]")

# Optional: Set the iTunes Connect Team ID if you have iOS app
# itc_team_id("[[ITC_TEAM_ID]]")

# Optional: Set the Developer Portal Team ID if you have iOS app
# team_name("[[DEV_PORTAL_TEAM_NAME]]")

# Optional: Set the platform to work with (android, ios, or both)
# platform("android")
